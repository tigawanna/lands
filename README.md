# lands website with next13.4 app directory
## status ⚠⚠ under development

configuration , add a .env.local file in the root directory use [.env.eaple](.env.example) as reference

configured routes `/admin/form`
